import Layout from "components/Layout";
import type { NextPage } from "next";

export const BlogPost: NextPage = () => (
	<Layout
		pageTitle={null}
		description="You can find projects JAred Rickert worked on and contact details on this site."
	>
		Blog Post
	</Layout>
);

export default BlogPost;
